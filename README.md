# chamah-manager-portal

פורטל מנהלות חמ״ה. כרגע מודול מחשבון השכר פעיל, ושאר המודולים משמשים כעמודי הכנה.

## הרצה מקומית

```bash
npm run build
npm start
```

## פרסום ב-Vercel

הפרויקט כולל `vercel.json`. Vercel יריץ `npm run build` ויגיש את תיקיית `dist`.
