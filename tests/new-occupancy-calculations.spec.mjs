import { test, expect } from '@playwright/test';
import { calculateOccupancyModel } from '../chamah-manager-portal/new/occupancy-calculations.js';
const active = { lifecycle_status: 'ACTIVE' };
const licensingRules = [{ ...active, age_group: 'INFANT', sqm_per_child: 2.8, max_children: 22, allowed_mixed_with: ['TODDLER'] }, { ...active, age_group: 'TODDLER', sqm_per_child: 2.6, max_children: 27, allowed_mixed_with: ['INFANT','GRADUATE'] }, { ...active, age_group: 'GRADUATE', sqm_per_child: 2.2, max_children: 33, allowed_mixed_with: ['TODDLER'] }];
const budgetRules = [{ ...active, standard_type: 'BASIC', age_group: 'INFANT', parameter_1: 6 }, { ...active, standard_type: 'BASIC', age_group: 'TODDLER', parameter_1: 9 }, { ...active, standard_type: 'BASIC', age_group: 'GRADUATE', parameter_1: 11 }];
const tuitionRules = [{ ...active, age_group: 'INFANT', numeric_value: 4000 }, { ...active, age_group: 'TODDLER', numeric_value: 3000 }, { ...active, age_group: 'GRADUATE', numeric_value: 2500 }];
test('calculates licensing, total staffing ceiling, revenue and optional wage', () => { const result = calculateOccupancyModel({ composition:{ INFANT:7,TODDLER:10 },area:50,standardType:'BASIC',hourlyWage:60,budgetRules,licensingRules,tuitionRules,monthlyOperatingHours:100 }); expect(result.requiredStaff).toBe(3); expect(result.requiredSqm).toBeCloseTo(45.6); expect(result.revenue).toBe(58000); expect(result.payrollCost).toBe(18000); expect(result.surplus).toBe(40000); });
test('rejects unapproved mix and supports efficiency without wage', () => { const result = calculateOccupancyModel({ composition:{ INFANT:2,GRADUATE:2 },area:20,standardType:'BASIC',budgetRules,licensingRules,tuitionRules,monthlyOperatingHours:100 }); expect(result.validMix).toBeFalsy(); expect(result.payrollCost).toBeNull(); expect(result.efficiencyScore).toBeGreaterThan(0); });
